class Server:

    def __init__(self, name, host, port, auth, username, password):
        self.name = name
        self.host = host
        self.port = port
        self.auth = auth
        self.username = username
        self.password = password

